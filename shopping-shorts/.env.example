# ComfyUI 주소. 내 PC면 그대로, Kaggle 이면 cloudflared 가 찍어준 https 주소
COMFY_URL=http://127.0.0.1:8188

# (선택) ffmpeg 경로를 직접 지정하고 싶을 때
# FFMPEG_BIN=C:\ffmpeg\bin\ffmpeg.exe

# (선택) Anthropic API 키 — write_shotlist.py --auto 에서만 사용. Claude Code 로 직접 받으면 불필요
ANTHROPIC_API_KEY=
