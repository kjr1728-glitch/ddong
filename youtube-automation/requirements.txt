google-api-python-client==2.*
# 유튜브 업로드용 OAuth 인증 (업로드는 API 키가 아니라 계정 로그인이 필요합니다)
google-auth-oauthlib==1.*
google-auth-httplib2==0.*
pytrends==4.*
# edge-tts는 마이크로소프트가 접속 방식을 바꿀 때마다 403 오류가 나는 일이 반복됩니다.
# 그때마다 최신 버전에서 대응이 들어가므로 버전을 묶지 않습니다.
# 403이 나면 먼저 pip install --upgrade edge-tts 를 해보세요.
edge-tts>=7.2
requests==2.*
python-dotenv==1.*
moviepy>=2.1
# moviepy 2.x는 자막을 Pillow로 직접 그리므로 ImageMagick을 따로 설치하지 않아도 되고,
# 영상 인코딩용 ffmpeg도 imageio-ffmpeg로 함께 설치됩니다.
# (1.0.3은 Pillow 10에서 삭제된 API를 쓰는 데다 최신 파이썬에서 설치가 자주 실패했습니다.)
imageio-ffmpeg>=0.5
Pillow>=10

# 선택사항 — scripts/script_writer.py로 스크립트까지 자동 생성할 때만 필요합니다.
# Claude Code에서 직접 스크립트를 받아 쓰면 설치하지 않아도 됩니다.
anthropic>=1.0
