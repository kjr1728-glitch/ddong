google-api-python-client==2.*
# 유튜브 업로드용 OAuth 인증 (업로드는 API 키가 아니라 계정 로그인이 필요합니다)
google-auth-oauthlib==1.*
google-auth-httplib2==0.*
pytrends==4.*
edge-tts==6.*
requests==2.*
python-dotenv==1.*
moviepy==1.0.3
# moviepy 1.0.3의 resize는 Pillow 10에서 삭제된 Image.ANTIALIAS를 사용합니다.
# Pillow 10 이상을 쓰면 영상 합성 단계에서 AttributeError로 멈추므로 9.x로 고정합니다.
Pillow>=9.5,<10

# 선택사항 — scripts/script_writer.py로 스크립트까지 자동 생성할 때만 필요합니다.
# Claude Code에서 직접 스크립트를 받아 쓰면 설치하지 않아도 됩니다.
anthropic>=1.0
